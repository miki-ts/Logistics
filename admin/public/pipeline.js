(()=>{const statuses=['Pending','In Review','Approved','Completed','Cancelled'],title=document.querySelector('#title'),content=document.querySelector('#content'),nav=document.querySelector('[data-view=pipeline]'),e=(t,x)=>{const n=document.createElement(t);n.textContent=x;return n};async function graph(){const data=await fetch('/api/admin/data').then(r=>r.json()),counts=statuses.map(s=>data.quotes.filter(q=>q.status===s).length),max=Math.max(1,...counts),box=document.createElement('section');box.className='chart';box.append(e('h2','Quote status overview'));const bars=document.createElement('div');bars.className='bars';statuses.forEach((s,i)=>{const b=document.createElement('div'),fill=document.createElement('i');b.className='bar';fill.style.height=(counts[i]/max*150)+'px';b.append(e('strong',counts[i]),fill,e('small',s));bars.append(b)});box.append(bars);content.append(box)}nav.addEventListener('click',async()=>{title.textContent='Quote pipeline';document.querySelectorAll('#nav button').forEach(x=>x.classList.toggle('active',x===nav));content.replaceChildren(e('p','Track every genuine quote request by its current approval stage.'));const board=document.createElement('section');board.className='pipeline';statuses.forEach(s=>{const lane=document.createElement('article'),head=document.createElement('header'),list=document.createElement('div');lane.className='lane';head.append(e('b',s),e('small','0'));lane.append(head,list);board.append(lane)});content.append(board);const data=await fetch('/api/admin/data').then(r=>r.json());statuses.forEach((s,i)=>{const lane=board.children[i],items=data.quotes.filter(q=>q.status===s);lane.querySelector('small').textContent=items.length;items.forEach(q=>{const card=document.createElement('div');card.className='quote-card';card.append(e('strong',q.name),e('span',q.service),e('p',q.message||'No cargo requirements provided'),e('small',new Date(q.createdAt).toLocaleDateString()));lane.querySelector('div').append(card)})})});document.querySelector('[data-view=overview]').addEventListener('click',()=>setTimeout(graph,80));setTimeout(graph,200)})();
